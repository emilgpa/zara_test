import styled from "styled-components";
import { HeaderHeight } from "./LayoutHeader.styled";

export const Root = styled.main`
  padding-top: ${HeaderHeight}px;
  padding-bottom: 50px;
  width: 100%;
`;
